/**
 * @description Funcion para inicializar el componente
 * @returns {Object} Devuelve la instancia del componente
 */
var componentDefault  = (function () {
	var init = function (element) {
		var element = element ? element : 'false';
	};
	return {
		init: init
	};
})();